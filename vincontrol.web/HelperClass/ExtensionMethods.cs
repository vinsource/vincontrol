using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;
using vincontrol.DomainObject;
using SelectListItem = System.Web.Mvc.SelectListItem;


namespace Vincontrol.Web.HelperClass
{
    public static class ExtensionMethods
    {
        //public static List<SelectListItem> ConvertToMvcSelectListItem(this List<ExtendedSelectListItem> list)
        //{
        //    return
        //        list.Select(i => new SelectListItem() {Selected = i.Selected, Text = i.Text, Value = i.Value}).ToList();
        //}
    }
}